import styles from "./gallery.module.scss";

export default function Gallery(){
  return(
    <section className="section--with-grid">
      <h2 className={styles.title}>Gallery Section -- on hold till I design how it should work.</h2>
    </section>
  )
}